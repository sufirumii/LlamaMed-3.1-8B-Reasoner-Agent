# Extra deps needed only to run `pytest tests/` (on top of requirements.txt).
pytest>=8.0.0
httpx>=0.27.0       # required by FastAPI's TestClient, used in test_server.py
reportlab>=4.0.0    # used to generate a throwaway test PDF in test_server.py
